# Copy this file to `.env` (or `.env.local`) for local development.
#
# Base URL of the Assuredia automation-engine backend (no trailing slash). The value is
# inlined into the bundle at build time, so a release build takes it from the process
# environment instead — see .env.production.
VITE_API_BASE_URL=http://localhost:8080
